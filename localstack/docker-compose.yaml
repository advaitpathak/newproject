version: "3.8"

services:
  localstack:
    image: localstack/localstack:latest
    ports:
      - "4566:4566" # LocalStack Gateway
    environment:
      - SERVICES=s3,lambda,dynamodb,opensearch
      - DEBUG=1
      - DATA_DIR=/var/lib/localstack/data
      - LAMBDA_EXECUTOR=docker
      - OPENSEARCH_DEFAULT_VERSION=2.13
    volumes:
      - "./localstack:/var/lib/localstack"
      - "/var/run/docker.sock:/var/run/docker.sock"


#version: "3.8"
#
#services:
#  localstack:
#    image: localstack/localstack
#    container_name: localstack
#    ports:
#      - "4566:4566"       # LocalStack Gateway (all services by default)
#      - "9200:9200"       # OpenSearch (legacy)
#      - "4572:4572"       # S3 (legacy)
#      - "4569:4569"       # DynamoDB (legacy)
#      - "4574:4574"       # Lambda (legacy)
#    environment:
#      - SERVICES=s3,lambda,dynamodb,opensearch
#      - DEBUG=1
#      - DATA_DIR=/var/lib/localstack/data
#      - LAMBDA_EXECUTOR=docker
#      - OPENSEARCH_DEFAULT_VERSION=2.13
#      - LOCALSTACK_USE_LEGACY_PORTS=1
#    volumes:
#      - "./localstack:/var/lib/localstack"
#      - "/var/run/docker.sock:/var/run/docker.sock"
